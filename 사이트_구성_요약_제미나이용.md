# Magic 지표 — 사이트·운영 통합 요약 (제미나이 공유용)

**문서 목적**: `magic-indicator-site` 정적 홈페이지와 `magic-indicator-api` 운영 정책의 현재 상태를 제미나이 등 외부 검토용으로 빠르게 전달하기 위한 요약이다. 법무 원문·금액·API 동작의 최종 기준은 각 HTML, API README, 서버 코드 원본을 우선한다.

## 1. 현재 큰 방향

| 항목 | 현재 기준 |
|------|-----------|
| 브랜드 | 주식회사 행복도담 인베스트 / Haengbokdodam Invest co.ltd |
| 상품 | MagicTrading 단일 전략 상품. TRV(TradingView)와 MT5 접근권을 판매 채널·플랫폼별로 구분 |
| 사이트 | 정적 HTML + 공통 CSS/JS. API 베이스는 `meta name="api-base"` 또는 로컬 개발 시 JS 자동 보정 |
| 백엔드 | `magic-indicator-api`: 회원, 결제, 쿠폰, 환불, 관리자, Ledger 자산, 크립토 입금 요청 처리 |
| 결제 원칙 | PayPal, 하나은행 외화송금, 크립토, 국내 원화 법인 송금의 4채널 |
| 환불 원칙 | 정규·비즈니스 월 플랜만 이유를 묻지 않는 31일·USD 일할 환불. 이벤트·쿠폰·무료체험·리커버리는 환불불가 |
| 크립토 원칙 | 메이저 코인 우선 지원. 기타 Ledger 지원 코인·밈코인·저유동성 코인은 견적 전 선송금 금지 |
| Ledger 원칙 | 수취·보관·잔고조회용. 시드·개인키·출금 권한 저장 없음. 자동 스왑·자동 출금 없음 |

## 2. 가격·플랜 표준

| 구분 | 이벤트 1개월 | 정규 월 |
|------|--------------|---------|
| Whop TRV | `$119` | `$5,499` |
| Whop MT5 | `$129` | `$5,999` |
| MQL MT5 | `$149` | `$6,999` |

- 정규는 연·분기·다월 선납·년회원 상품을 두지 않고 월 1회 납부를 원칙으로 한다.
- 쿠폰으로 이용 기간이 늘어나는 것은 결제 주기와 별개다.
- 7일 무료 체험은 자동 유료 청구되지 않는다.
- 1개월 유료 이벤트는 안내·결제·활성화 이후 환불불가로 별도 고지한다.

## 3. 결제 채널 정리

홈페이지 공개 문구는 `세 갈래`가 아니라 4채널로 정리했다.

| 채널 | 용도 |
|------|------|
| PayPal | 소액, 이벤트, 가입 단계 카드 결제 중심. PayPal 정책에 따라 잔액·페이류 제한 가능 |
| 하나은행 외화송금 | 고액, 정규, 해외·외화 직접 송금, 인보이스 중심 |
| 크립토 | 온체인 직접 송금. 메이저 우선 지원, 기타 코인은 수동 견적 |
| 국내 원화 법인 송금 | 국내 원화 납부. 당사 법인 계좌와 인보이스 기준 |

문구 정합성 정리:

- `메이저 자동`은 과하게 보일 수 있어 `메이저 우선 지원`으로 정리.
- `모든 코인 지원`은 실제 거절 가능성과 충돌하므로 `메이저 우선·기타 코인 견적 정책`으로 정리.
- 공개 페이지의 주소 표는 실제 송금 기준이 아니라 `인보이스 안내` 기준으로 표시.
- XRP의 `Tag 필수` 표현은 체인·거래소 조건에 따라 달라질 수 있어 `Tag/메모는 인보이스 기준`으로 정리.
- Airwallex는 고객 직접 송금 채널이 아니라 PayPal 정산용으로만 사용한다고 정리.

## 4. 크립토·Ledger 정책

메이저 우선 지원 코인:

- BTC, ETH, USDT, USDC, XRP, SOL, TRX, BNB, MATIC
- 내부 Ledger 자산관리에는 Base, Arbitrum, Optimism, Avalanche C-Chain, Dogecoin, Litecoin 주소도 등록됨

기타·잡코인 처리:

- Ledger 지원 코인, 신규 코인, 밈코인, 저유동성 토큰은 먼저 보내면 안 된다.
- 먼저 코인명, 네트워크, 예정 수량으로 견적 요청을 받는다.
- 운영이 유동성, 매도 가능 여부, 스왑 수수료, 네트워크 수수료를 확인한다.
- 견적 유효시간과 수취 방식을 안내한 뒤 송금한다.
- 실제 결제 승인은 입금 시점 표시가가 아니라 실제 `USDT 스왑 완료액` 기준이다.
- 허니팟, 매도 불가, 출금·스왑 곤란, 극저유동성 토큰은 접수 후에도 거절 가능하다.
- Ledger는 자동 스왑 장치가 아니다. 입금 감지·운영 알림은 자동화할 수 있지만 출금·스왑은 운영자가 확인하고 직접 승인한다.

Ledger 자산관리 현황:

- 관리자 보드에 `Ledger 자산` 탭이 있다.
- 로컬 개발에서는 MongoDB 없이 `magic-indicator-api/data/ledger-portfolio-local.json`으로 폴백 저장한다.
- 등록된 주소/xpub과 수동 수량·USDT 단가를 기반으로 포트폴리오를 보여준다.
- 지원 체인은 서버가 잔고·가격·환율 조회를 시도하고, 실패·미지원 자산은 수동값으로 평가한다.
- stablecoin인 USDT/USDC는 단가가 비어 있으면 `1 USDT` 기준으로 보정한다.
- TRC-20 USDT, EVM 토큰, BTC, XRP, SOL, TRX, DOGE, LTC 등 조회 로직이 추가되어 있다.

등록된 주요 Ledger 네트워크:

| 표시 | 네트워크 |
|------|----------|
| BTC | Bitcoin |
| ETH | Ethereum / ERC-20 |
| TRON | Tron / TRC-20 |
| SOL | Solana |
| XRP | XRP Ledger |
| BNB | BNB Smart Chain / BEP-20 |
| POLY | Polygon |
| Base | Base |
| Arbitrum | Arbitrum One |
| Optimism | OP Mainnet / Optimism |
| Avalanche C-Chain | Avalanche C-Chain |
| Dogecoin | Dogecoin |
| Litecoin | Litecoin |

## 5. 환불·법무 요지

| 항목 | 기준 |
|------|------|
| 정규·비즈니스 월 | 이유를 묻지 않는 31일·USD 일할 환불 |
| 사용일 | 구간 시작일부터 환불 신청 당일까지, 신청 당일 포함 |
| 해외 수취 | USD 지급 원칙 |
| 국내 수취 | 동일 USD 환불액을 기준으로 당시 운영 안내에 따라 원화 입금 가능 |
| 이벤트·쿠폰·체험·리커버리 | 환불불가 |
| 1개월 유료 이벤트 | 환불·취소·대가 반환 불가로 별도 강하게 고지 |

주의:

- `환불불가(원칙)`과 `절대 환불 불가`가 같이 쓰인다. 전자는 이벤트성 혜택 일반 원칙, 후자는 1개월 유료 이벤트의 별도 강한 예외 고지다.
- 정규 환불과 이벤트 환불불가를 혼동하지 않도록 각 페이지에서 `정규·비즈니스 월만`이라고 반복 고지한다.

## 6. 주요 페이지 구조

| 경로 | 역할 |
|------|------|
| `index.html` | 랜딩, 상품 설명, 결제 4채널 요약, 가격표, Q&A |
| `billing/index.html` | 구독·결제, 4채널 납부, 환불 요청, 크립토 입금 알림, USDT 네트워크 참조 |
| `events/index.html` | 무료 체험, 1개월 이벤트, 확정 가격, 환불불가 안내 |
| `downloads/index.html` | TRV/MT5 다운로드·접근권·플랜 매트릭스 |
| `registration/index.html` | 회원가입, 본인인증, OTP, 추천인 |
| `verify/index.html` | 본인 인증 |
| `integrations/index.html` | TRV/MT5 계정·서버 조합, 연동 설명 |
| `contact/index.html` | 문의·버그·티켓 |
| `legal/*` | 이용약관, 환불, 이벤트 특약, 정규 특약, 개인정보, 접근 매트릭스 |
| `admin/index.html` | 관리자 보드, 입금, 쿠폰, 환불, Ledger 자산 |
| `admin/monitor.html` | 관리자 모니터 |
| `404.html` | 없는 URL 안내 |

## 7. 빌드·검증

레포: `magic-indicator-site`

| 명령 | 역할 |
|------|------|
| `npm run build:nav` | 상단 내비를 `scripts/inject-site-nav.mjs`의 `PAGES` 기준으로 주입 |
| `npm run build:seo` | `sitemap.xml`, `robots.txt` 생성. `admin/`은 색인 제외 |
| `npm run build:og` | SNS 크롤러용 `og:image` 절대 URL 처리 |
| `npm run lint:html` | HTML 검증 |
| `npm run lint:links` | HTML 간 `.html` 링크 존재 검사 |
| `npm run build` / `npm run verify` | 전체 빌드·검증 |

최근 검증:

- `npm run lint:html`
- 결과: `files=30 errors=0 warnings=0`

## 8. 최근 문장 패러독스 감사 결과

| 기존 문제 | 수정 방향 |
|-----------|-----------|
| 홈에서 `세 갈래(초안)`이라고 하면서 실제로는 4채널을 설명 | `납부·결제는 네 갈래`로 수정 |
| `준비중 · 채택 전 초안`처럼 현재 운영과 맞지 않는 홈 문구 | `운영 안내 · 결제 채널`로 수정 |
| `모든 코인 지원`과 `거절 가능`이 동시에 존재 | `메이저 우선·기타 코인 견적 정책`으로 수정 |
| `메이저 자동` 표현이 실제 수동 대조와 충돌 | `메이저 우선 지원`으로 수정 |
| Ledger 관리자 화면이 `도착 전 준비 화면`이라고 표시 | 현재 상태에 맞게 `Ledger 자산현황`으로 수정 |
| 법무 요약의 `묻지 않는(묻지 않는)` 중복 | `이유를 묻지 않는 일할 환불`로 정리 |
| XRP `Tag 필수`가 모든 상황에 고정된 것처럼 보임 | `Tag/메모는 인보이스 기준`으로 수정 |
| `PayPal+Airwallex` 정산 표현이 결제 흐름을 혼동시킴 | Airwallex는 PayPal 정산용, 고객 직접 결제·송금은 PayPal/하나은행/크립토/원화로 수정 |

남겨둔 표현:

- `준비중`, `미연동`은 실제 PG ON/OFF 상태 표시나 다운로드 URL 미연결 안내에 해당하므로 유지 가능.
- `환불불가(원칙)`과 `절대 환불 불가`는 법무 고지 강도 차이로 유지 가능.

## 9. 제미나이에 요청할 때 유용한 질문

1. 이 홈페이지의 결제·환불·크립토 정책 문구 사이에 사용자 오해를 만들 수 있는 모순이 남아 있는가?
2. `메이저 우선 지원·기타 코인 수동 견적` 정책이 법무·결제·환불 페이지에서 일관되게 보이는가?
3. `정규 월 환불 가능`과 `이벤트 환불불가`의 구분이 충분히 명확한가?
4. Ledger 관련 문구가 사용자가 자동 스왑이나 자동 출금을 기대하지 않도록 충분히 제한적인가?
5. 공개 페이지에서 실제 수취 주소를 숨기고 인보이스 기준으로 안내하는 방식이 결제 오인 가능성을 줄이는가?

## 10. 파일·스크립트 위치

| 구분 | 경로 |
|------|------|
| 본 요약 | `magic-indicator-site/사이트_구성_요약_제미나이용.md` |
| 공개 사이트 | `magic-indicator-site/*.html`, `magic-indicator-site/**/index.html` |
| 공통 JS | `magic-indicator-site/assets/*.js` |
| 공통 CSS | `magic-indicator-site/assets/*.css` |
| 관리자 UI | `magic-indicator-site/admin/index.html`, `assets/admin-app.js` |
| API 서버 | `magic-indicator-api/server.js` |
| 관리자 API | `magic-indicator-api/src/admin-routes.js` |
| Ledger 서비스 | `magic-indicator-api/src/ledger-portfolio-service.js` |
| 로컬 Ledger 원장 | `magic-indicator-api/data/ledger-portfolio-local.json` |

---

**갱신일**: **2026-04-30**. 이번 갱신 키워드: 결제 4채널, 가격 6종 표준, 크립토 수동 견적, 잡코인 선송금 금지, USDT 스왑 완료액 정산, Ledger 자산관리, 자동 스왑 금지, 문장 패러독스 정리, HTML 린트 통과.
# Magic 지표 — 사이트·운영 통합 요약 (제미나이 공유용)

**문서 목적**: `magic-indicator-site` 정적 홈페이지와 `magic-indicator-api` 운영 정책의 현재 상태를 제미나이 등 외부 검토용으로 빠르게 전달하기 위한 요약이다. 법무 원문·금액·API 동작의 최종 기준은 각 HTML, API README, 서버 코드 원본을 우선한다.

## 1. 현재 큰 방향

| 항목 | 현재 기준 |
|------|-----------|
| 브랜드 | 주식회사 행복도담 인베스트 / Haengbokdodam Invest co.ltd |
| 상품 | MagicTrading 단일 전략 상품. TRV(TradingView)와 MT5 접근권을 판매 채널·플랫폼별로 구분 |
| 사이트 | 정적 HTML + 공통 CSS/JS. API 베이스는 `meta name="api-base"` 또는 로컬 개발 시 JS 자동 보정 |
| 백엔드 | `magic-indicator-api`: 회원, 결제, 쿠폰, 환불, 관리자, Ledger 자산, 크립토 입금 요청 처리 |
| 결제 원칙 | PayPal, 하나은행 외화송금, 크립토, 국내 원화 법인 송금의 4채널 |
| 환불 원칙 | 정규·비즈니스 월 플랜만 이유를 묻지 않는 31일·USD 일할 환불. 이벤트·쿠폰·무료체험·리커버리는 환불불가 |
| 크립토 원칙 | 메이저 코인 우선 지원. 기타 Ledger 지원 코인·밈코인·저유동성 코인은 견적 전 선송금 금지 |
| Ledger 원칙 | 수취·보관·잔고조회용. 시드·개인키·출금 권한 저장 없음. 자동 스왑·자동 출금 없음 |

## 2. 가격·플랜 표준

현재 홈페이지와 약관의 대표 가격은 아래 6개로 통일한다.

| 구분 | 이벤트 1개월 | 정규 월 |
|------|--------------|---------|
| Whop TRV | `$119` | `$5,499` |
| Whop MT5 | `$129` | `$5,999` |
| MQL MT5 | `$149` | `$6,999` |

운영 기준:

- 정규는 연·분기·다월 선납·년회원 상품을 두지 않고 월 1회 납부를 원칙으로 한다.
- 쿠폰으로 이용 기간이 늘어나는 것은 결제 주기와 별개다.
- 7일 무료 체험은 자동 유료 청구되지 않는다.
- 1개월 유료 이벤트는 안내·결제·활성화 이후 환불불가로 별도 고지한다.

## 3. 결제 채널 정리

홈페이지 공개 문구는 `세 갈래`가 아니라 4채널로 정리했다.

| 채널 | 용도 |
|------|------|
| PayPal | 소액, 이벤트, 가입 단계 카드 결제 중심. PayPal 정책에 따라 잔액·페이류 제한 가능 |
| 하나은행 외화송금 | 고액, 정규, 해외·외화 직접 송금, 인보이스 중심 |
| 크립토 | 온체인 직접 송금. 메이저 우선 지원, 기타 코인은 수동 견적 |
| 국내 원화 법인 송금 | 국내 원화 납부. 당사 법인 계좌와 인보이스 기준 |

문구 정합성 정리:

- `메이저 자동`은 과하게 보일 수 있어 `메이저 우선 지원`으로 정리.
- `모든 코인 지원`은 실제 거절 가능성과 충돌하므로 `메이저 우선·기타 코인 견적 정책`으로 정리.
- 공개 페이지의 주소 표는 실제 송금 기준이 아니라 `인보이스 안내` 기준으로 표시.
- XRP의 `Tag 필수` 표현은 체인·거래소 조건에 따라 달라질 수 있어 `Tag/메모는 인보이스 기준`으로 정리.

## 4. 크립토·Ledger 정책

메이저 우선 지원 코인:

- BTC, ETH, USDT, USDC, XRP, SOL, TRX, BNB, MATIC
- 추가로 내부 Ledger 자산관리에는 Base, Arbitrum, Optimism, Avalanche C-Chain, Dogecoin, Litecoin 주소도 등록됨

기타·잡코인 처리:

- Ledger 지원 코인, 신규 코인, 밈코인, 저유동성 토큰은 먼저 보내면 안 된다.
- 먼저 코인명, 네트워크, 예정 수량으로 견적 요청을 받는다.
- 운영이 유동성, 매도 가능 여부, 스왑 수수료, 네트워크 수수료를 확인한다.
- 견적 유효시간과 수취 방식을 안내한 뒤 송금한다.
- 실제 결제 승인은 입금 시점 표시가가 아니라 실제 `USDT 스왑 완료액` 기준이다.
- 허니팟, 매도 불가, 출금·스왑 곤란, 극저유동성 토큰은 접수 후에도 거절 가능하다.
- Ledger는 자동 스왑 장치가 아니다. 입금 감지·운영 알림은 자동화할 수 있지만 출금·스왑은 운영자가 확인하고 직접 승인한다.

Ledger 자산관리 현황:

- 관리자 보드에 `Ledger 자산` 탭이 있다.
- 로컬 개발에서는 MongoDB 없이 `magic-indicator-api/data/ledger-portfolio-local.json`으로 폴백 저장한다.
- 등록된 주소/xpub과 수동 수량·USDT 단가를 기반으로 포트폴리오를 보여준다.
- 지원 체인은 서버가 잔고·가격·환율 조회를 시도하고, 실패·미지원 자산은 수동값으로 평가한다.
- stablecoin인 USDT/USDC는 단가가 비어 있으면 `1 USDT` 기준으로 보정한다.
- TRC-20 USDT, EVM 토큰, BTC, XRP, SOL, TRX, DOGE, LTC 등 조회 로직이 추가되어 있다.

등록된 주요 Ledger 네트워크:

| 표시 | 네트워크 |
|------|----------|
| BTC | Bitcoin |
| ETH | Ethereum / ERC-20 |
| TRON | Tron / TRC-20 |
| SOL | Solana |
| XRP | XRP Ledger |
| BNB | BNB Smart Chain / BEP-20 |
| POLY | Polygon |
| Base | Base |
| Arbitrum | Arbitrum One |
| Optimism | OP Mainnet / Optimism |
| Avalanche C-Chain | Avalanche C-Chain |
| Dogecoin | Dogecoin |
| Litecoin | Litecoin |

## 5. 환불·법무 요지

| 항목 | 기준 |
|------|------|
| 정규·비즈니스 월 | 이유를 묻지 않는 31일·USD 일할 환불 |
| 사용일 | 구간 시작일부터 환불 신청 당일까지, 신청 당일 포함 |
| 해외 수취 | USD 지급 원칙 |
| 국내 수취 | 동일 USD 환불액을 기준으로 당시 운영 안내에 따라 원화 입금 가능 |
| 이벤트·쿠폰·체험·리커버리 | 환불불가 |
| 1개월 유료 이벤트 | 환불·취소·대가 반환 불가로 별도 강하게 고지 |

주의:

- 법무 문구에서 `환불불가(원칙)`과 `절대 환불 불가`가 같이 쓰인다. 전자는 이벤트성 혜택 일반 원칙, 후자는 1개월 유료 이벤트의 별도 강한 예외 고지다.
- 정규 환불과 이벤트 환불불가를 혼동하지 않도록 각 페이지에서 `정규·비즈니스 월만`이라고 반복 고지한다.

## 6. 주요 페이지 구조

| 경로 | 역할 |
|------|------|
| `index.html` | 랜딩, 상품 설명, 결제 4채널 요약, 가격표, Q&A |
| `billing/index.html` | 구독·결제, 4채널 납부, 환불 요청, 크립토 입금 알림, USDT 네트워크 참조 |
| `events/index.html` | 무료 체험, 1개월 이벤트, 확정 가격, 환불불가 안내 |
| `downloads/index.html` | TRV/MT5 다운로드·접근권·플랜 매트릭스 |
| `registration/index.html` | 회원가입, 본인인증, OTP, 추천인 |
| `verify/index.html` | 본인 인증 |
| `integrations/index.html` | TRV/MT5 계정·서버 조합, 연동 설명 |
| `contact/index.html` | 문의·버그·티켓 |
| `legal/*` | 이용약관, 환불, 이벤트 특약, 정규 특약, 개인정보, 접근 매트릭스 |
| `admin/index.html` | 관리자 보드, 입금, 쿠폰, 환불, Ledger 자산 |
| `admin/monitor.html` | 관리자 모니터 |
| `404.html` | 없는 URL 안내 |

## 7. 빌드·검증

레포: `magic-indicator-site`

| 명령 | 역할 |
|------|------|
| `npm run build:nav` | 상단 내비를 `scripts/inject-site-nav.mjs`의 `PAGES` 기준으로 주입 |
| `npm run build:seo` | `sitemap.xml`, `robots.txt` 생성. `admin/`은 색인 제외 |
| `npm run build:og` | SNS 크롤러용 `og:image` 절대 URL 처리 |
| `npm run lint:html` | HTML 검증 |
| `npm run lint:links` | HTML 간 `.html` 링크 존재 검사 |
| `npm run build` / `npm run verify` | 전체 빌드·검증 |

최근 검증:

- `npm run lint:html`
- 결과: `files=30 errors=0 warnings=0`

## 8. 최근 문장 패러독스 감사 결과

최근 전체 홈페이지 문구를 감사해 아래 충돌을 수정했다.

| 기존 문제 | 수정 방향 |
|-----------|-----------|
| 홈에서 `세 갈래(초안)`이라고 하면서 실제로는 4채널을 설명 | `납부·결제는 네 갈래`로 수정 |
| `준비중 · 채택 전 초안`처럼 현재 운영과 맞지 않는 홈 문구 | `운영 안내 · 결제 채널`로 수정 |
| `모든 코인 지원`과 `거절 가능`이 동시에 존재 | `메이저 우선·기타 코인 견적 정책`으로 수정 |
| `메이저 자동` 표현이 실제 수동 대조와 충돌 | `메이저 우선 지원`으로 수정 |
| Ledger 관리자 화면이 `도착 전 준비 화면`이라고 표시 | 현재 상태에 맞게 `Ledger 자산현황`으로 수정 |
| 법무 요약의 `묻지 않는(묻지 않는)` 중복 | `이유를 묻지 않는 일할 환불`로 정리 |
| XRP `Tag 필수`가 모든 상황에 고정된 것처럼 보임 | `Tag/메모는 인보이스 기준`으로 수정 |
| `PayPal+Airwallex` 정산 표현이 결제 흐름을 혼동시킴 | Airwallex는 PayPal 정산용, 고객 직접 결제·송금은 PayPal/하나은행/크립토/원화로 수정 |

남겨둔 표현:

- `준비중`, `미연동`은 실제 PG ON/OFF 상태 표시나 다운로드 URL 미연결 안내에 해당하므로 유지 가능.
- `환불불가(원칙)`과 `절대 환불 불가`는 법무 고지 강도 차이로 유지 가능.

## 9. 제미나이에 요청할 때 유용한 질문

검토 요청 예시:

1. 이 홈페이지의 결제·환불·크립토 정책 문구 사이에 사용자 오해를 만들 수 있는 모순이 남아 있는가?
2. `메이저 우선 지원·기타 코인 수동 견적` 정책이 법무·결제·환불 페이지에서 일관되게 보이는가?
3. `정규 월 환불 가능`과 `이벤트 환불불가`의 구분이 충분히 명확한가?
4. Ledger 관련 문구가 사용자가 자동 스왑이나 자동 출금을 기대하지 않도록 충분히 제한적인가?
5. 공개 페이지에서 실제 수취 주소를 숨기고 인보이스 기준으로 안내하는 방식이 결제 오인 가능성을 줄이는가?

## 10. 파일·스크립트 위치

| 구분 | 경로 |
|------|------|
| 본 요약 | `magic-indicator-site/사이트_구성_요약_제미나이용.md` |
| 공개 사이트 | `magic-indicator-site/*.html`, `magic-indicator-site/**/index.html` |
| 공통 JS | `magic-indicator-site/assets/*.js` |
| 공통 CSS | `magic-indicator-site/assets/*.css` |
| 관리자 UI | `magic-indicator-site/admin/index.html`, `assets/admin-app.js` |
| API 서버 | `magic-indicator-api/server.js` |
| 관리자 API | `magic-indicator-api/src/admin-routes.js` |
| Ledger 서비스 | `magic-indicator-api/src/ledger-portfolio-service.js` |
| 로컬 Ledger 원장 | `magic-indicator-api/data/ledger-portfolio-local.json` |

---

**갱신일**: **2026-04-30**. 이번 갱신 키워드: 결제 4채널, 가격 6종 표준, 크립토 수동 견적, 잡코인 선송금 금지, USDT 스왑 완료액 정산, Ledger 자산관리, 자동 스왑 금지, 문장 패러독스 정리, HTML 린트 통과.
# Magic 지표 — 사이트·운영 통합 요약 (제미나이·문서 공유용)

**문서 목적**: `magic-indicator-site` 정적 서비스의 **페이지 구조**, **표현 톤**, **운영 보드(API)**, **UI·빌드 방향**, **실제 코드 반영 범위**를 한 파일에서 파악할 수 있게 정리한다. 법무·결제 원문 세부는 각 HTML 또는 API 레포 원본을 본다.

| 항목 | 내용 |
|------|------|
| **브랜드** | 주식회사 행복도담 인베스트 / Haengbokdodam Invest co.ltd |
| **현장 기술** | 정적 HTML + 공통 CSS(JS). API 연동 베이스: `meta name="api-base"` (예: `https://happyinvests.com`) |
| **백엔드 레포** | `magic-indicator-api` — 라우트·ACL·결제(PayPal 등)·쿠폰·티켓은 별도 참고 |
| **결제 채택(표기)** | 공개 카피·빌링 UI는 **PayPal · 하나은행 외화송금 · 크립토 · 원화 법인송금** 중심. Airwallex는 PayPal 정산용으로만 표기 |
| **HTML 규모** | 루트 기준 **29개** `*.html`(빌드·린트·링크 검사 대상). `404.html` 은 내비 **`PAGES` 비포함**·감사 시 **고아 예외**만 |

---

## 0. 빌드·검증(레포 `magic-indicator-site`)

| 스크립트 / 명령 | 역할 |
|-----------------|------|
| **`PAGES`** (`scripts/inject-site-nav.mjs`) | 상단 `<nav class="site-nav">` 의 **단일 정의 원천**. `kind`·`active` 로 현재 탭·상대 경로 렌더. |
| **`npm run build:nav`** | 각 HTML 에 내비 주입. 디스크의 `*.html` 과 매니페스트 **정합 감사**(누락=오류, 고아=경고). **`404.html`** 만 고아 **예외**(내비 비대상). |
| **`npm run build:seo`** | **`sitemap.xml`** · **`robots.txt`** 작성. **`SITE_ORIGIN`**(기본 `https://happyinvests.com`). **`admin/` 는 사이트맵 제외**. **`robots.txt`** 에 **`Disallow: /admin/`**. 사이트맵 각 URL에 **`lastmod`(빌드일 YYYY-MM-DD)**. |
| **`npm run build:og`** (`scripts/rewrite-og-absolute.mjs`) | **`og:image`** 를 페이지 경로 기준 **`SITE_ORIGIN` 절대 URL** 로 치환(SNS 크롤러는 JS 미실행이 일반적). 이미 `https` 면 재실행해도 유지(`data-relative` 제거됨). |
| **`npm run lint:html`** | `html-validate` · `.htmlvalidate.json` |
| **`npm run lint:links`** (`scripts/check-links.mjs`) | 페이지 간 **`.html` 링크** 존재 여부만 검사(CSS·CDN 제외 패턴 고려). |
| **`npm run build`** · **`verify`** | `build:nav` → `build:seo` → `build:og` → `lint:html` → `lint:links` 순(배포·PR 게이트). |

- **`inject-site-nav.mjs`** 및 **`rewrite-og-absolute.mjs`** 등은 **파일 자체가 CLI 진입점일 때만** 전체 처리; `generate-seo-files.mjs` 가 `inject-site-nav.mjs` 를 **import 할 때만** `PAGES` 를 쓰고 HTML 은 수정하지 않는다(`main()` 게이트).
- **`README.md`**: 명령·`SITE_ORIGIN`·호스트별 **`404.html` 매핑** 안내·프런트 UX 요약.
- **SEO**: 주요 페이지 `canonical`·`og`·`twitter:card` 등. 메인 **Organization JSON-LD**. 배포 후 **`SITE_ORIGIN`** 과 생성물 URL 일치 확인.

### 송금·결제 카피(공개 페이지만)

- **`billing/index.html#swift-gap-remedy`**: SWIFT 등 **송금 공백기** — 증빙(MT103 등) 검토 후 **입금 매칭 전 이용 권한** 가능 안내 등. **자동 임시 라이선스** 는 API·운영 영역.
- **메인** 등에서 해당 앵커로 짧게 연결 가능.

---

## 「완결」을 피한 이유 — 개선 여지(제미나이 맥락용)

목표를 **건전함·통일 상한(~80점대 체감)** 에 둠. 레거시 정적 HTML 특성상 **인라인 스타일·일부 접근 규칙 린트 완화**, **CSP / 외부 SDK SRI / 라이트하우스 극값 / 전 페이지 메타 완전 동일** 등은 **의도적으로 미완 과제**로 남김(API·운영 일정과 분리).

---

## 1. 사이트맵 (대표 HTML)

| 경로 | 역할 |
|------|------|
| `index.html` | 랜딩: 매니페스토·퀵뷰·플랜·모임터·Q&A·게시판 임베드·`#main-content`(퀵뷰~) |
| `guide/*` | 가이드 허브, TRV/MT5 사용, Magic R 보조 |
| `downloads/index.html` | 다운로드·플랜 표·MT5 패키지(정규 OTP 미완 시 배너 JS) |
| `registration/index.html` · `verify/index.html` | 가입·SMS·OTP·본인인증 (`registration`: `<main id="main-content">` , 리드 카피 **일반 이용자 중심**·**API 표시 숨김**) |
| `billing/index.html` | 구독·결제·월납 정책·환불 요청 등. **상단 페이지 목차**(앵커)·**`<main id="main-content">`**. **납부 요약 배너**는 내부 API 경로 노출 없이 일반인용 문구 |
| `events/index.html` · `membership/index.html` · `reflection/index.html` | 이벤트·혜택·실전 후기 |
| `contact/index.html` | 문의·버그(티켓). **`<main id="main-content">`** |
| `telegram-chat-id/index.html` | 텔레그램 Chat ID 헬프 |
| `boards/index.html` | 모임터(탭=게시 카테고리). **연결 정보**는 `details` 로 개발·점검용 |
| `integrations/index.html` | TRV/MT5·웹훅 등 |
| `legal/*` | 약관·환불·플랜 총괄·접근 행렬·MagicTrading 특약 |
| `admin/index.html` · `admin/monitor.html` | 관리자 보드 · 플랜 모니터. **`meta robots: noindex, nofollow`**. **사이트맵 미수록** · **robots `Disallow`** |
| `404.html` | 없는 URL 안내 메인 복귀. **내비 주입 안 함**(매니페스트 비대상). |

**도구**: `tools/ledger-xpub-receive/` — Ledger xPub 파생 등(개별 README).

---

## 2. 공통 UI · 프런트 동작

- **헤더/푸터·`site-nav`**: 전 페이지 **동일 순서**(메인 → 가이드·지표 · … → 추천·홍보 → 약관 → 관리자 → 연동). 현재 페이지 `aria-current`. 메인 `#home-pages-title` 허브와 순서 정렬.
- **`site-ux.js`**: 스킵 링크(대상 **`#main-content`**)·**크롬바**(테마·다국어 `data-i18n`)·토스트·`fetchWithRetry`. **테마** 시 **`meta theme-color` / Apple 상태 표시줄** 동기화, **`referrer`**, **`viewport-fit=cover`** 보강, 지원 브라우저 **테마 View Transitions**(모션 줄임 사용자는 생략). 런타임 **`og:image` 보정**은 크롤러 없을 수 있음 → **빌드 `build:og` 가 진실**.
- **`data-magic-board`** + **`board-embed.js`** + `MagicAuth`.
- **`html[data-theme="dark"]`**: 라이트/다크 전환(`happyinvest-theme`·시스템 선호 연동 가능).

### 디자인 · UX 패턴 요약

- **글꼴**: CDN **Pretendard** + **Inter** (`--font-sans`).
- **스킵 링크** (`site-ux.css`): **`focus-visible`** 중심으로 화면에 노출(MOUS만 포커스 시 과한 노출 완화).
- **폼 일관성**: `:root` **`accent-color`**, **`color-scheme: light`**(다크는 `site-ux`). **`scrollbar-gutter: stable`**. **`.page-title`** 에 **`text-wrap: balance`**(지원 브라우저).
- **모바일 내비**: 좁은 폭에서 **`site-nav` 링크** 최소 터치 높이에 가깝게.
- **노치·세이프 영역**: **`.ux-chrome-bar`** 패딩에 **`env(safe-area-inset-*)`**.
- **메인 히어로 이미지**(`index.html`): **`fetchpriority="high"`**, **`decoding="async"`**, **`viewport`** 에 **`viewport-fit=cover`** 명시 가능.
- **미니멀 인터랙션**·전역 **`prefers-reduced-motion`** 존중.
- 브랜드 **`--ci-green`** 유지. **`:focus-visible`** 최소 적용 유지(`common.css`).

---

## 3. 백오피스 · 운영(관리자) 요약

**인증**: `/api/admin/*` JWT 등(세부는 API).

### 게시판 카테고리(ACL)

| slug | 접근 요지 |
|------|-----------|
| `admin_payment_paypal` / `_airwallex` / `_crypto` | 관리자 전용 — 입금 확인 로그 |
| `admin_free_coupon` | 관리자 전용 — 무료 쿠폰 추적 |
| 공개 카테고리 | `BOARD_ACL`·`acl.js` 참고 |

### 입금 확인 / 무료 쿠폰 / 티켓 등

- 입금 채널별 **텔레그램**(환경변수)·**표 파싱**(`admin-app.js`, 플랫폼 별칭 정규화).
- 확인 시 회원에게 **SMTP / 선택 SMS(`user_notify`)** 규칙.
- **티켓** `in_progress`·`resolved`, **대시보드** `ticket_bug_stale_48h` 등(기존 요약과 동일·API README 우선).

### 소규모 팀(근로자 2명, 실무 집중 1인)·운영 현실

| 축 | 하는 일 |
|----|---------|
| **SSOT** | 등급·혜택·제한은 **`legal/*`·약관 허브** 한 축. **개정일** 한 줄 병기 권장. |
| **점검** | **분기 1회** 또는 **요금·이벤트·ACL 변경 직후** 정합만. |
| **4줄 체크** | 역할·라이선스 → 결제/만료 안내 → 민감 처리 **한 줄 기록** → 반복 문의는 **고정글** |
| **비상** | 동료 1명과 **권한 묶음 1~2개**(관리 로그인·결제 콘솔 등)만 공유. |
| **템플릿** | 가입/변경/불일치/제재/SSOT 링크 **복붙 5종**. |
| **예외** | **월 상한** + 스프레드시트 **한 열** 기록. |

---

## 4. 메인(`index.html`) 요지(원문 톤 보존)

- 투자 권유·수익 보장 **아님** — **판단 참고·교육**.
- TRV·MT5 동시 타깃, 플랜·월납·가입 정책(SMS·정규 OTP) 등은 HTML 원문.

---

## 5. `downloads/index.html` 보안 안내 레이어

- 비로그인 시 해당 로직 스킵.
- `regular_default` 이고 **`google_otp_enabled` 미완** 시 배너(`.ex5` 게이트는 API·정책과 연동).

---

## 6. 제한사항

이 파일은 **요약**이다. 금액·법조문 전문은 **`legal/*.html`** 및 API 원본 우선.

---

## 7. 파일·스크립트 위치

| 구분 | 경로 |
|------|------|
| **본 요약** | `magic-indicator-site/사이트_구성_요약_제미나이용.md` |
| 내비·매니페스트 감사 | `scripts/inject-site-nav.mjs` |
| SEO 생성 | `scripts/generate-seo-files.mjs` |
| OG 절대 경로 | `scripts/rewrite-og-absolute.mjs` |
| HTML·링크 린트 | `scripts/lint-html.mjs`, `scripts/check-links.mjs` |
| 정적 404 | `404.html` — 호스트에서 404 페이지로 지정 |
| API 레포 참고 | `magic-indicator-api/README.txt` 등 |

---

**갱신일**: **2026-04-28**. 이번 정돈에 포함된 키워드 일람: **`PAGES`/내비** · 빌드 체인 **`build:seo`·`build:og`·lint** · **사이트맵(`admin` 제외·`lastmod`)** · **`robots`·관리자 `noindex`** · **`404.html`·고아 예외** · **절대 `og:image`** · **회원가입/결제/문의·랜드마크·빌링 목차·배너 카피** · **브라우저 최신 패턴**(테마 표시줄·View Transitions·safe-area·accent·스킵링크 등) · **소규모 팀 운영 절** · **README 교차 참조**.
